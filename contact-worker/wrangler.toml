name = "double-rooted-contact"
main = "worker.js"
compatibility_date = "2024-09-01"

# GMAIL_APP_PASSWORD is stored as a secret (never in this file).
# Set it once with: npx wrangler secret put GMAIL_APP_PASSWORD
